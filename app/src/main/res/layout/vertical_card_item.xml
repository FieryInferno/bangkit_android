<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingBottom="16dp"
    android:background="@drawable/rounded_linear_layout">
    <ImageView
        android:id="@+id/img_vertical_item"
        android:layout_width="108dp"
        android:layout_height="114dp"
        android:scaleType="centerCrop"
        android:background="@drawable/top_rounded_linear_layout"
        tools:src="@tools:sample/backgrounds/scenic"
        android:contentDescription="@string/card_image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />
    <TextView
        android:id="@+id/tv_title_vertical_item"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="8dp"
        android:paddingTop="8dp"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        tools:text="Konten 1"
        app:layout_constraintTop_toBottomOf="@id/img_vertical_item"
        app:layout_constraintStart_toStartOf="parent"
        />
    <TextView
        android:id="@+id/tv_subtitle_vertical_item"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="8dp"
        android:paddingTop="4dp"
        android:textSize="11sp"
        android:textColor="@color/black"
        tools:text="Subtitel Kontent 2"
        app:layout_constraintTop_toBottomOf="@id/tv_title_vertical_item"
        app:layout_constraintStart_toStartOf="parent"
        />


</androidx.constraintlayout.widget.ConstraintLayout>